{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": "src"
  },
  // Los tests se chequean con `pnpm typecheck`, pero no tienen por que viajar
  // dentro del artefacto que se despliega.
  "exclude": ["node_modules", "dist", "test", "**/*.spec.ts", "**/__tests__/**"]
}
